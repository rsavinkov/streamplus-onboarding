<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}StreamPlus - Subscription Service{% endblock %}</title>

    {# Bootstrap 5 CSS #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    {# Custom CSS #}
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {# Font Awesome (optional) #}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="d-flex flex-column min-vh-100">

{# Main Content #}
<main class="container flex-grow-1">

    {# Page Content Block #}
    {% block body %}{% endblock %}
</main>

{# Footer #}
<footer class="bg-dark text-white py-4 mt-4">
    <div class="container">
        <div class="row">
            <div class="col-md-4">
                <h5>StreamPlus</h5>
                <p>Your premium streaming service with the best content.</p>
            </div>
        </div>
        <hr class="my-4 bg-secondary">
        <div class="text-center">
            &copy; {{ 'now'|date('Y') }} StreamPlus. All rights reserved.
        </div>
    </div>
</footer>

{# JavaScript at the end of the document #}
{% block javascripts %}
    {# Bootstrap 5 JS Bundle with Popper #}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

    {# Custom JS #}
    {{ encore_entry_script_tags('app') }}
{% endblock %}
</body>
</html>